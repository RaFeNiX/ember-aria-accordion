{{yield (hash
  panel=(component 'accordion-panel'
  active=active
  toggle=toggle
  multiple=multiple
  refresh=refresh
  flags=flags
  activePanels=activePanels
  select=(action 'selectAccordion')
  focusAccordion=(action 'focusAccordion')
  ))  }}
